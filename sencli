#!/bin/bash
# Sentio CLI shortcut script
# Usage: ./sencli [arguments...]

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Execute sentio_cli with all passed arguments
exec "${SCRIPT_DIR}/build/sentio_cli" "$@"
